# 测试结果 · 2026-09-21

## 0.1.3 步行与地图

37项领域测试通过；18项设备回归17通过、1拨号器缺失跳过。真实接口专项增加公共车站入口步行路线和静态地图解码，已通过。Lint 0错误、17条依赖更新/风格警告。范围与未验证项目见 `WALKING_013.md`。

## 0.1.2 真实查询与界面复测

- 35项领域测试通过，0失败。新增步行时间完整性、铁路空占位字段兼容、跨接口站点编号匹配回归；站名与坐标及完整站序一致才补全方向。
- 高德真实接口专项测试1项通过：使用北京西站、北京大学人民医院等公共地标，实际验证地点搜索、地铁路线解析与筛选、步行时间、完整站序方向、坐标转换及逆地理编码。未使用用户家庭地址或手机位置。
- Demo设备回归14项：13通过、1跳过、0失败。跳过项仍是模拟器缺少拨号器；含下车站大字、时间分项、去除分段米数、完整换乘、提醒关闭、其他应用保持前台等检查。
- Lint 0错误、13警告；真实模式Debug构建成功。家庭交付APK使用amap模式，本地Key不随源码压缩包分发。
- 人工检查竖屏截图：`screenshots/route-012-portrait.png`、`screenshots/reminder-012-portrait.png`。截图使用演示路线，非真实行程。

运行方式：真实专项使用 `-PROUTE_PROVIDER=amap`、测试类 `LiveApiSmokeTest` 及 `liveApi=true`；界面与兼容回归使用 `-PROUTE_PROVIDER=demo`，选择其余三个测试类。真实专项默认不自动运行，避免无意调用服务。

仍未验证：老人手机实际定位与通话、真实乘车提醒时机、厂商后台限制。以下0.1.1及首版记录保留为历史结果。

## 0.1.1 兼容性加固复测

`gradlew.bat test lint assembleDebug connectedDebugAndroidTest` 成功。32项领域测试保持通过；13项设备测试中12项通过、1项跳过、0失败；Lint为0错误、13条依赖更新/风格警告。

新增通过的5项专项检查：已安装 APK 权限白名单及组件出口、系统设置在提醒到来时保持前台且可回桌面、提醒弹窗返回键关闭不推进行程、通知结束行程后释放通知/当前唤醒锁、提醒不改变音量/音频模式/铃声/勿扰且无全屏 Intent。

拨号界面检查跳过：MuMu Android 15 镜像对 ACTION_DIAL 返回 No activity found。没有拨出任何电话，也没有验证真实接听/拒接、蓝牙或网络通话。具体范围与手机验收要求见 `COEXISTENCE.md`。

资源检查迭代中修正了一项测试误判：dumpsys power 的历史记录包含过去使用的锁，不能直接当作当前持锁；改为检查 Wake Locks 当前列表，系统证据显示已释放。复测通过。

下面保留0.1.0首次交付的基线结果。

## 最终结果

| 检查 | 结果 |
| --- | --- |
| JVM 领域测试 | 32通过，0失败 |
| Android 15 模拟器测试 | 7通过，0失败，0跳过 |
| Android Lint | 0错误，13警告 |
| Debug APK | assembleDebug 成功 |
| 标准 Gradle Wrapper | build-local.ps1 执行成功 |
| APK 安装/启动 | MuMu Android 15 安装成功，应用可启动 |

环境：Windows 11，Microsoft OpenJDK 17.0.18，Android SDK 36 / Build Tools 35.0.0，Gradle 8.13，AGP 8.11.1，Kotlin 2.1.20。设备为本机 MuMu Android 15 模拟器；系统报告型号 NCO-AL00，**并非物理手机**。

执行命令：`gradlew.bat test lint assembleDebug connectedDebugAndroidTest`。最终通过后使用 `adb install -r` 安装交付 APK，再以 `am start -W` 验证启动。

## 已覆盖内容

23项核心测试覆盖：有无地铁、步行分类及合计、路线耗时上限、200米容差的稳定排序、换乘优先、缺站点过滤、可靠方向判断、最近地点去重、固定地点排除、提醒计算、逐站时间、单站立即提醒、站均时间约束、防重复、通知未启用、换乘状态、序列化恢复、重启失效、过期真实路线及跨城市过滤。

9项高德解析测试覆盖：虚构官方字段样本、步行/站数/出口、普通公交剔除、步行字段缺失、总步行不一致、缺途经站、终点不一致、空数组兼容、坐标有效性和不从名称猜方向。未调用真实 Key。

7项模拟器测试：

1. 首页固定地点、地点确认、唯一路线、步行提示。
2. 文字搜索同名目的地，显示两个候选，未确认不规划。
3. 两段地铁完整行程、两次上车、两次提醒弹窗、确认后换乘/出站。
4. 演示叫车按钮不会发送虚构路线。
5. 系统无语音服务时给出提示，仍能文字搜索。
6. 8个并发提醒检查只投递一次通知；再次检查不更新通知投递时间。
7. 未授予定位权限时，从可见应用开启前台服务，切到系统首页后仍触发提醒。

13条 Lint 警告由11条依赖/Gradle版本更新建议和2条 Uri.parse 可改为 KTX 扩展的风格建议构成。未禁用 Lint，未添加基线隐藏错误。

## 迭代中修复

- 第一轮站均时间测试的期望值计算有误，修正为确实触发90秒下限的样本。
- Windows SDK 路径冒号按 properties 规则转义，消除 Lint 错误。
- 增加 Android 12+ 显式禁用云备份及设备迁移规则。
- 增加当前路线过期/位置改变/断网检查，防止直接启动旧路线。
- 宽屏截图检查后将内容区域限制为600dp并居中。

## 未验证

真实高德返回、真实城市/路线、真实高德唤端、API 26设备、API 36设备、物理手机锁屏/静音/震动强度/厂商省电、实际地铁提醒时机。对这些项目不宣称通过，具体清单见 `ACCEPTANCE.md`。
